// Script to show string_sort.
#include <iostream>

void swap_values(int& a, int& b)
{
  if (a != b)
    {
      a ^= b;
      b ^= a;
      a ^= b;
    }
}

void selection_sort(int a[], int size)
{
  
}

void string_sort(char str[], int n)
{
  int arr[n];
  for (int i = 0; i < n; i++)
    arr[i] = static_cast<int>(char[i]);
  
}
